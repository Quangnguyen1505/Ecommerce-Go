server:
  port: 8082
databases:
  - user: mydb
    password: mydb
    host: localhost
    dbName : mydb
  - user: devdb
    password: devdb
    host: localhost
    dbName : devdb
security:
  jwt:
    secret: mysecret
